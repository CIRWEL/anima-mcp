# Pi credentials envelope — copy to envelope.pi and fill in
# envelope.pi is gitignored

PI_PASSWORD="your-pi-password"
PI_USER="unitares-anima"
PI_HOST="192.168.1.165"
SSH_KEY="${HOME}/.ssh/id_ed25519_pi"
